Reconstruct forensic timelines for every case directory under `/app/data/work/`. The normative contract is `/app/data/docs/CONTRACT.md`. A disclosed fit pack is at `/app/data/fit/alpha/` (ledgers plus `/app/data/fit/alpha/smoke_digests.json`). An almost-correct multi-module helper package is shipped under `/app/engine/` (orchestration in `pipeline.py`, with a thin `starter.py` re-export). That package is fit-smoke-calibrated: its digests under `smoke_digests.json` are sha256 of the JSON text the almost-correct pipeline emits for the fit ledgers, not graded normative expecteds for work cases. Induce the closed rules from the contract, including cross-ledger rename coalesce after chronological merge and SI-only residual adoption that respects poisoned paths, use the fit ledgers and engine smoke as a non-normative check, then apply the full contract to each work case. Held cases may compose multiple rules on one long timeline and are not isomorphic copies of the fit pack.

For each work case `<id>`, write `/app/output/<id>/timeline.json` and `/app/output/<id>/ownership.json` exactly as specified in the contract (schemas, sort orders, identity pairing of slot with gen, rename coalesce to MOVE including across a wrap seam and across ledger origin when adjacent in chronological order, journal wall authority, circular oplog wrap with txnlog gap fills, stream non-inheritance across gen, path poison with no revive including on SI-only object-table adoption, orphan `RENAME_NEW`, clean DELETE-then-CREATE transfers, and SI-only residual incarnations with zero journal events). Also write `/app/output/corpus_index.json` for the whole work corpus as specified in the contract.

Success criteria:
1. Every work case directory name under `/app/data/work/` has both `/app/output/<id>/timeline.json` and `/app/output/<id>/ownership.json`.
2. Each `/app/output/<id>/timeline.json` matches the contract schema, key order, and JSON text shape.
3. Each `/app/output/<id>/ownership.json` matches the contract schema, key order, sorting, and JSON text shape.
4. Timeline event identities, kinds, times, and ordering match the contract for every work case.
5. Ownership incarnation names, streams, and `poisoned_paths` match the contract for every work case.
6. `/app/output/corpus_index.json` matches the contract schema and aggregates for every work case under `/app/data/work/`.
